kind: pipeline
name: default

steps:
  - name: build
    image: dvitali/builder-tools:latest
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands:
      - bash ./build.sh
      - bash ./build.sh -e
